# Speaker Recognition Demo

## Usage

1. Edit `config.yaml` and fill in your data paths and hyperparameters.
   
2. Run `main.sh` to automatically execute the full pipeline. All experimental results will be saved automatically.

## Quick Start

```bash
pip install -r requirements.txt
bash main.sh
